{% extends 'base.html.twig' %}

{% block title %}Edition de produit{% endblock %}

{% block body %}

    <div class="container text-center">

    <h1>Edition de produit</h1>

    {{ include('product/_form.html.twig', {'button_label': 'Modifier'}) }}

    <a href="{{ path('manager_product') }}" class="btn btn-warning">Retour à la liste des produits</a>

    {{ include('product/_delete_form.html.twig') }}
    
    </div>
{% endblock %}
